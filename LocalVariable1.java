package Basicprograms;

public class LocalVariable1 {

	public static void main(String[] args) {
		
		// variable --->  is a counter or name where we store the value.
		// int a =5,b=6,c; ----> you can declare the variable of same type .
		// but we can declare the int type of variable only.
		// Type of variable 
		//Local :- it can be declare inside the static method , non static method, constructor and main method.
		// the scope of the LV is inside the method only and it work inside the method only.
		// It will not work outside the method
		//int a ;// local variable
// Some rule for Local variable--->it is mandatory we have to initialize it, if not then then it will show show compile time error
		// Local variable ---> we are not using any access specifier
		int a =6;
		// OR int a ;
		// a=6;
		System.out.println(a);
	}
		

	}


